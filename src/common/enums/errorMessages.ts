export enum ERROR_MESSAGES {
  CREATE_USER_FAILED = 'User Creation Process Failed',
  EMAIL_VERIFICATION_NOT_FOUND = "Verification ID doesn't exists",
  USER_NOT_FOUND = 'User not found',
  USER_ALREADY_IN_DB = 'That email is already in use',
  MISSING_TOKEN_SECRET = 'Missing Access Token Secret',
}
